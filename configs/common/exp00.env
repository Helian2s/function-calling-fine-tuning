# Shared Experiment 0 runtime constants for shell entry points.
# Source this file from scripts instead of duplicating image tags or paths.

FT_AWS_REGION="${FT_AWS_REGION:-us-west-2}"
FT_S3_BUCKET="${FT_S3_BUCKET:-finetuning-lab-1-037678282394-us-west-2-an}"
FT_S3_PREFIX="${FT_S3_PREFIX:-finetuning}"
FT_EXPERIMENT="${FT_EXPERIMENT:-exp-00}"

AUTOMODEL_IMAGE="${AUTOMODEL_IMAGE:-nvcr.io/nvidia/nemo-automodel:25.11.00}"
AUTOMODEL_EXPECTED_VERSION="${AUTOMODEL_EXPECTED_VERSION:-0.3.0}"
CUDA_TEST_IMAGE="${CUDA_TEST_IMAGE:-nvidia/cuda:12.8.1-base-ubuntu24.04}"

HOST_WORKSPACE_ROOT="${HOST_WORKSPACE_ROOT:-/mnt/workspace}"
HOST_PROJECT_ROOT="${HOST_PROJECT_ROOT:-${HOST_WORKSPACE_ROOT}/project}"
HOST_DATA_ROOT="${HOST_DATA_ROOT:-${HOST_WORKSPACE_ROOT}/data}"
HOST_HF_CACHE_ROOT="${HOST_HF_CACHE_ROOT:-${HOST_WORKSPACE_ROOT}/huggingface-cache}"
HOST_NGC_CACHE_ROOT="${HOST_NGC_CACHE_ROOT:-${HOST_WORKSPACE_ROOT}/ngc-cache}"
HOST_CHECKPOINT_ROOT="${HOST_CHECKPOINT_ROOT:-${HOST_WORKSPACE_ROOT}/checkpoints}"
HOST_RESULTS_ROOT="${HOST_RESULTS_ROOT:-${HOST_WORKSPACE_ROOT}/results}"
HOST_LOGS_ROOT="${HOST_LOGS_ROOT:-${HOST_WORKSPACE_ROOT}/logs}"
HOST_RUN_INFO_ROOT="${HOST_RUN_INFO_ROOT:-${HOST_WORKSPACE_ROOT}/run-info}"

CONTAINER_PROJECT_ROOT="${CONTAINER_PROJECT_ROOT:-/workspace/project}"
CONTAINER_DATA_ROOT="${CONTAINER_DATA_ROOT:-/workspace/data}"
CONTAINER_CHECKPOINT_ROOT="${CONTAINER_CHECKPOINT_ROOT:-/workspace/checkpoints}"
CONTAINER_RESULTS_ROOT="${CONTAINER_RESULTS_ROOT:-/workspace/results}"
CONTAINER_LOGS_ROOT="${CONTAINER_LOGS_ROOT:-/workspace/logs}"
CONTAINER_RUN_INFO_ROOT="${CONTAINER_RUN_INFO_ROOT:-/workspace/run-info}"

SMOKE_CONFIG_PATH="${SMOKE_CONFIG_PATH:-configs/exp00_smoke/smoke_qlora.yaml}"
SMOKE_MODEL_NAME="${SMOKE_MODEL_NAME:-Qwen/Qwen3-8B}"
SMOKE_MODEL_REVISION="${SMOKE_MODEL_REVISION:-b968826d9c46dd6066d109eabc6255188de91218}"
SMOKE_ADAPTER_PATH="${SMOKE_ADAPTER_PATH:-/workspace/checkpoints/exp-00/smoke-qlora}"
SMOKE_BASELINE_LIMIT="${SMOKE_BASELINE_LIMIT:-20}"
SMOKE_MAX_NEW_TOKENS="${SMOKE_MAX_NEW_TOKENS:-256}"
SMOKE_SEED="${SMOKE_SEED:-42}"
